---
phase: 06-settings-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - SEC-06

must_haves:
  truths:
    - "README explains why the project was built in first-person narrative"
    - "README identifies target audience (who is this for)"
    - "README has a Security section with emoji risk indicators linking to SECURITY.md"
    - "README documents Linux/macOS as primary platform with Windows as secondary"
    - "README documents [P] Preferences menu and available settings"
    - "README reflects current project state: native Claude Code installer, GitHub CLI, CLI + GUI variants"
  artifacts:
    - path: "README.md"
      provides: "Full project README for open-source release"
      contains: "Claude Sandbox Manager"
      min_lines: 250
  key_links:
    - from: "README.md"
      to: "SECURITY.md"
      via: "Link in Security section"
      pattern: "SECURITY\\.md"
    - from: "README.md"
      to: "LICENSE"
      via: "Badge and Notes reference"
      pattern: "Apache.*2\\.0"
---

<objective>
Complete rewrite of README.md as a proper open-source project README with badges, narrative sections, cross-platform documentation, configuration docs, security summary, and updated content reflecting the full v1 feature set.

Purpose: The README is the project's front door -- it must communicate what the project is, why it exists, who it's for, and how to use it. The current README is outdated and Windows-centric.
Output: README.md (complete rewrite, ~250-350 lines)
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-documentation/06-CONTEXT.md
@.planning/phases/06-settings-documentation/06-RESEARCH.md

<!-- Need to reference 06-01 SUMMARY for settings details and 06-02 SUMMARY for SECURITY.md content -->
@.planning/phases/06-settings-documentation/06-01-SUMMARY.md
@.planning/phases/06-settings-documentation/06-02-SUMMARY.md

<interfaces>
<!-- README must accurately document these features -->

Settings menu (from 06-01):
- [P] Preferences in main menu
- Settings: auto-backup toggle, default container type, memory limit, CPU limit, MCP port
- Config file: csm-config.json (auto-created with defaults)

Security (from 06-02):
- SECURITY.md exists with risk table
- Risk indicators: 🟢 Hardened, 🟡 User responsibility

Current menu actions: [S] Start, [T] Stop, [N] New, [R] Remove, [B] Backup, [E] Restore, [P] Preferences, [Q] Quit

Container variants: CLI (minimal), GUI (Xfce + noVNC + Chromium)

Entry points: bin/csm (Linux/macOS), claude-manager.bat (Windows)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete README.md rewrite</name>
  <files>README.md</files>
  <action>
    Complete rewrite of README.md. Do NOT preserve old content -- write fresh from the locked section order.

    **Section order (per locked decision):**
    1. **Badges** -- shields.io static badges: platform (Linux/macOS/Windows), license (Apache 2.0), Docker required
    2. **Title + Description** -- "Claude Sandbox Manager" with 2-3 sentence description of what it does
    3. **Why I built this** -- First-person narrative, personal + practical tone. Safety motivation ("wanted to let Claude Code loose without worrying about my machine"), productivity angle (multiple isolated environments), convenience angle (no Docker expertise needed). 3-5 paragraphs.
    4. **Who is this for** -- Bullet list: developers exploring AI coding assistants, people cautious about running AI agents on their main machine, teams wanting reproducible sandbox environments, anyone using Claude Code who wants isolation without complexity
    5. **Prerequisites** -- Table: Docker Desktop or Docker Engine, Git, SSH client. Note Linux/macOS have SSH built in. Link to Docker download.
    6. **Quick Start** -- Two subsections:
       - **Linux / macOS**: clone repo (use github.com/Jacob101mcd/claude-sandbox-manager.git URL), `chmod +x bin/csm`, `./bin/csm`, auto-creates default instance, SSH in
       - **Windows**: clone repo, double-click `claude-manager.bat`, same flow
       Include the `--dangerously-skip-permissions` command for running Claude Code inside
    7. **Multi-Instance Support** -- Show the updated menu output (including [P] Preferences and [B] Backup, [E] Restore), explain each action briefly, show instance resources table
    8. **Configuration** -- Document [P] Preferences menu: what settings are available (auto-backup, default container type, memory limit, CPU limit, MCP port), config stored in csm-config.json, auto-created with sensible defaults
    9. **Integrations** -- Keep existing MCP Toolkit and Remote Control sections (already accurate from Phase 5), update port override note to reference [P] Preferences instead of .env
    10. **Security** -- Brief summary with emoji risk indicator table (5 rows matching SECURITY.md), link to SECURITY.md for full analysis. Mention --dangerously-skip-permissions trade-off briefly.
    11. **What's Included** -- Updated container contents: Ubuntu 24.04, Node.js LTS, Claude Code (native installer), GitHub CLI, SSH server. Two variants: Minimal CLI, GUI Desktop (Xfce + noVNC + Chromium)
    12. **SSH Details** -- Table with host alias, host, port, user, auth, key type (keep from current README, still accurate)
    13. **Notes** -- SSH keys auto-generated, workspace migration note, .instances.json persistence, license reference (Apache 2.0)

    **Target length:** 250-350 lines
    **Tone:** Friendly, practical, not overly technical. First person in "Why" section, standard docs tone elsewhere.
    **Key change from old README:** Linux/macOS first, Windows second. bin/csm is primary entry point. Repo URL is claude-sandbox-manager. No more PowerShell-centric framing.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "Why I built this" README.md && grep -q "Who is this for" README.md && grep -q "Security" README.md && grep -q "SECURITY.md" README.md && grep -q "Preferences" README.md && grep -q "dangerously" README.md && wc -l README.md | awk '{if ($1 >= 250 && $1 <= 400) print "PASS"; else print "WARN: " $1 " lines"}'</automated>
  </verify>
  <done>README.md is a complete rewrite with all 13 required sections, badges, Linux/macOS-first platform ordering, configuration docs for [P] Preferences, security summary linking to SECURITY.md, updated container contents, 250-350 lines</done>
</task>

</tasks>

<verification>
- `grep -q "Why I built this" README.md` confirms narrative section
- `grep -q "Who is this for" README.md` confirms audience section
- `grep -q "SECURITY.md" README.md` confirms security link
- `grep -q "dangerously-skip-permissions" README.md` confirms security disclaimer
- `grep -q "Apache" README.md` confirms license reference
- `grep -q "bin/csm" README.md` confirms Linux/macOS primary
- `grep -q "Preferences" README.md` confirms settings documentation
- `wc -l README.md` is between 250-400
</verification>

<success_criteria>
- README has all 13 sections in the locked order
- "Why I built this" is first-person narrative about safety + productivity
- "Who is this for" identifies target audience
- Security section has emoji risk table and links to SECURITY.md
- Linux/macOS documented first, Windows second
- Configuration section documents [P] Preferences and available settings
- Container contents reflect native Claude Code installer, GitHub CLI, two variants
- 250-350 lines target met
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-documentation/06-03-SUMMARY.md`
</output>
