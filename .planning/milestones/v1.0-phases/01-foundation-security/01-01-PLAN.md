---
phase: 01-foundation-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/Dockerfile
  - .dockerignore
  - tests/test_helper.bash
  - tests/dockerfile.bats
  - tests/security.bats
autonomous: true
requirements:
  - SEC-01
  - QUAL-02
  - QUAL-04

must_haves:
  truths:
    - "Dockerfile contains no hardcoded passwords"
    - "Dockerfile uses passwordless sudo for claude user"
    - "Docker build context excludes large directories"
    - "ShellCheck and BATS are available for testing"
  artifacts:
    - path: "scripts/Dockerfile"
      provides: "Secured container definition"
      contains: "NOPASSWD"
    - path: ".dockerignore"
      provides: "Build context exclusions"
      contains: "workspaces"
    - path: "tests/test_helper.bash"
      provides: "BATS test helper with project root resolution"
    - path: "tests/dockerfile.bats"
      provides: "Dockerfile security tests"
    - path: "tests/security.bats"
      provides: "Security hardening verification tests"
  key_links:
    - from: "tests/dockerfile.bats"
      to: "scripts/Dockerfile"
      via: "grep assertions on Dockerfile content"
      pattern: "grep.*Dockerfile"
---

<objective>
Harden the Dockerfile (remove hardcoded password, add passwordless sudo) and set up the test infrastructure (BATS + ShellCheck) that all subsequent plans depend on.

Purpose: SEC-01 is a critical security fix. Test infrastructure enables automated verification for all later plans. .dockerignore prevents slow builds.
Output: Secured Dockerfile, .dockerignore, BATS test framework ready with initial security tests.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-security/01-CONTEXT.md
@.planning/phases/01-foundation-security/01-RESEARCH.md
@.planning/phases/01-foundation-security/01-VALIDATION.md

<interfaces>
<!-- Existing Dockerfile that needs security fixes -->
From scripts/Dockerfile (current, insecure):
```dockerfile
# Line 20 - MUST BE REPLACED:
RUN useradd -m -s /bin/bash claude \
    && echo 'claude:claude123' | chpasswd \
    && adduser claude sudo
```

Expected replacement (from RESEARCH.md):
```dockerfile
RUN useradd -m -s /bin/bash claude \
    && usermod -aG sudo claude \
    && echo 'claude ALL=(ALL) NOPASSWD:ALL' > /etc/sudoers.d/claude \
    && chmod 0440 /etc/sudoers.d/claude
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test tooling and create BATS test infrastructure</name>
  <files>tests/test_helper.bash, tests/dockerfile.bats, tests/security.bats</files>
  <action>
1. Install ShellCheck and BATS:
   ```
   sudo apt-get update && sudo apt-get install -y shellcheck
   ```
   For BATS, clone bats-core from https://github.com/bats-core/bats-core.git into a temp location and run `sudo ./install.sh /usr/local`. Verify with `bats --version`.

2. Create `tests/test_helper.bash`:
   - Set `CSM_ROOT` to the project root (resolve from test file location: `$(cd "$(dirname "$BATS_TEST_FILENAME")/.." && pwd)`)
   - Export `CSM_ROOT`

3. Create `tests/dockerfile.bats` with tests:
   - Test: "Dockerfile has no hardcoded password" -- `! grep -q 'chpasswd' "$CSM_ROOT/scripts/Dockerfile"`
   - Test: "Dockerfile uses NOPASSWD sudo" -- `grep -q 'NOPASSWD' "$CSM_ROOT/scripts/Dockerfile"`
   - Test: "Dockerfile does not set root as final USER" -- last USER line should be `root` (for sshd), but claude user is created with proper sudo

4. Create `tests/security.bats` with placeholder tests for later plans:
   - Test: "lib/docker.sh binds SSH to localhost" -- skip if file doesn't exist yet (use `[[ -f "$CSM_ROOT/lib/docker.sh" ]] || skip`)
   - Test: "lib/docker.sh drops capabilities" -- skip if file doesn't exist yet
   - Test: "lib/docker.sh sets memory limit" -- skip if file doesn't exist yet
   - Test: "lib/docker.sh sets CPU limit" -- skip if file doesn't exist yet
   - Test: "lib/docker.sh sets no-new-privileges" -- skip if file doesn't exist yet

Each security.bats test should grep for the specific string in lib/docker.sh: `127.0.0.1`, `cap-drop`, `memory=2g`, `cpus=2`, `no-new-privileges`.
  </action>
  <verify>
    <automated>cd /home/claude/workspace/claude-sandbox-manager && bats tests/</automated>
  </verify>
  <done>BATS runs successfully; dockerfile.bats tests initially fail (Dockerfile not yet fixed); security.bats tests skip (lib/docker.sh doesn't exist yet)</done>
</task>

<task type="auto">
  <name>Task 2: Harden Dockerfile and create .dockerignore</name>
  <files>scripts/Dockerfile, .dockerignore</files>
  <action>
1. Edit `scripts/Dockerfile`:
   - Replace the insecure user creation block (lines 19-21):
     FROM: `RUN useradd -m -s /bin/bash claude && echo 'claude:claude123' | chpasswd && adduser claude sudo`
     TO:
     ```dockerfile
     RUN useradd -m -s /bin/bash claude \
         && usermod -aG sudo claude \
         && echo 'claude ALL=(ALL) NOPASSWD:ALL' > /etc/sudoers.d/claude \
         && chmod 0440 /etc/sudoers.d/claude
     ```
   - Verify no other references to passwords exist in the file.
   - Do NOT change anything else in the Dockerfile -- the SSH setup, host key baking, and GSD installation must remain intact.

2. Create `.dockerignore` at project root with:
   ```
   workspaces/
   backups/
   ssh/
   .git/
   .planning/
   tests/
   *.md
   .instances.json
   ```
   Note: Do NOT exclude `_build_ssh/` -- it is needed during build (COPY directive). Do NOT exclude `scripts/` -- the Dockerfile is in there.

3. Run `shellcheck` on any .sh files if they exist (they won't yet, but verify the command works): `shellcheck bin/csm lib/*.sh 2>/dev/null || true`
  </action>
  <verify>
    <automated>cd /home/claude/workspace/claude-sandbox-manager && bats tests/dockerfile.bats && ! grep -q 'chpasswd' scripts/Dockerfile && grep -q 'NOPASSWD' scripts/Dockerfile && test -f .dockerignore</automated>
  </verify>
  <done>Dockerfile contains NOPASSWD sudo with no hardcoded passwords. .dockerignore excludes workspaces/, backups/, ssh/, .git/, .planning/. All dockerfile.bats tests pass green.</done>
</task>

</tasks>

<verification>
- `bats tests/dockerfile.bats` -- all tests pass
- `bats tests/security.bats` -- all tests skip (pending lib/docker.sh from Plan 03)
- `! grep -q 'chpasswd' scripts/Dockerfile` -- no hardcoded password
- `grep -q 'NOPASSWD' scripts/Dockerfile` -- passwordless sudo present
- `.dockerignore` exists and excludes large directories
</verification>

<success_criteria>
- Dockerfile has no hardcoded passwords (SEC-01 addressed)
- BATS test framework installed and running
- Test files exist with passing/skipping tests
- .dockerignore prevents large build contexts (QUAL-02 partial)
- ShellCheck available for linting (QUAL-04 partial)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security/01-01-SUMMARY.md`
</output>
